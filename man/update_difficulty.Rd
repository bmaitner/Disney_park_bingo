% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/difficulty.R
\name{update_difficulty}
\alias{update_difficulty}
\title{Update difficulty estimates from player feedback}
\usage{
update_difficulty(squares, feedback, prior_weight = 10)
}
\arguments{
\item{squares}{A squares table, e.g. from \code{\link[=bingo_squares]{bingo_squares()}}.}

\item{feedback}{A data frame with columns \code{id} and \code{crossed} (logical), one
row per square per played card, e.g. rows bound from \code{\link[=record_feedback]{record_feedback()}}.}

\item{prior_weight}{How many observations the initial guess is worth.}
}
\value{
The squares table with updated \code{p_crossed} and \code{n_feedback}.
}
\description{
Refines each square's \code{p_crossed} with observed outcomes. The current
estimate is treated as worth \code{prior_weight + n_feedback} observations, so
early guesses move quickly with real data and well-tested estimates stay
stable:
}
\details{
\code{p_new = (p_crossed * (prior_weight + n_feedback) + crossed) / (prior_weight + n_feedback + shown)}
}
\examples{
squares <- bingo_squares()
card <- make_bingo_card(squares, seed = 1)
fb <- record_feedback(card, crossed = card$squares$id[1:12])
updated <- update_difficulty(squares, fb)
}
