% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/card.R
\name{make_bingo_card}
\alias{make_bingo_card}
\title{Make a bingo card}
\usage{
make_bingo_card(
  squares = bingo_squares(),
  mode = c("cynic", "fan", "mixed"),
  age = c("adult", "child"),
  park = "any",
  difficulty = "any",
  size = 5,
  free_space = TRUE,
  balance = TRUE,
  park_weight = 2,
  seed = NULL
)
}
\arguments{
\item{squares}{A squares table, e.g. from \code{\link[=bingo_squares]{bingo_squares()}}.}

\item{mode}{\code{"cynic"} or \code{"fan"} keep squares of that mode plus those tagged
\code{"any"}; \code{"mixed"} keeps all modes.}

\item{age}{\code{"adult"} keeps all squares; \code{"child"} keeps only child-suitable
squares.}

\item{park}{A park name or code (see \code{\link[=park_code]{park_code()}}). \code{"any"} keeps only
squares that can happen anywhere; a specific park adds its own squares.}

\item{difficulty}{How hard the card should be. Levels tilt the mix of
squares drawn from the easiest, middle, and hardest thirds (by
\code{p_crossed}) of the eligible squares, so every setting can make a card:
\code{"any"} draws evenly (8/8/8 on a 5x5 card), \code{"easy"} favors easy squares
(12/8/4), \code{"medium"} favors the middle (4/16/4), and \code{"hard"} favors hard
squares (4/8/12). A numeric \code{c(min, max)} instead keeps only squares with
\code{p_crossed} in that range.}

\item{size}{Number of rows (and columns). Defaults to 5.}

\item{free_space}{Put a free space in the center (odd sizes only)?}

\item{balance}{With \code{difficulty = "any"} or a numeric range, \code{TRUE} draws
evenly across the easiest, middle, and hardest thirds so that cards made
together are comparably hard; \code{FALSE} draws at random. Difficulty levels
always use their tilted mix.}

\item{park_weight}{Relative chance of drawing a park-specific square versus
a square that can happen anywhere, when \code{park} is a specific park.}

\item{seed}{Optional random seed for a reproducible card.}
}
\value{
An object of class \code{bingo_card}: a list with \code{grid} (character
matrix of labels, \code{NA} for the free space), \code{ids} (matrix of square ids),
\code{squares} (the drawn rows of the squares table with \code{row} and \code{col}
positions), and the settings used.
}
\description{
Randomly draws squares matching the requested mode, age, park, and
difficulty, and lays them out on a square grid with an optional free space
in the center.
}
\examples{
card <- make_bingo_card(mode = "cynic", park = "Magic Kingdom", seed = 1)
card
}
\seealso{
\code{\link[=make_bingo_cards]{make_bingo_cards()}}, \code{\link[=save_bingo_cards]{save_bingo_cards()}}, \code{\link[=bingo_odds]{bingo_odds()}}
}
