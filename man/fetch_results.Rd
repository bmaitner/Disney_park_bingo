% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inbox.R
\name{fetch_results}
\alias{fetch_results}
\title{Fetch finished-card results from players}
\usage{
fetch_results(
  endpoint = Sys.getenv("PARKBINGO_ENDPOINT"),
  token = Sys.getenv("PARKBINGO_TOKEN"),
  min_minutes = 0
)
}
\arguments{
\item{endpoint}{The inbox's Apps Script web app URL. Defaults to the
\code{PARKBINGO_ENDPOINT} environment variable.}

\item{token}{The private read token created by \code{createReadToken()} in the
script. Defaults to the \code{PARKBINGO_TOKEN} environment variable.}

\item{min_minutes}{Drop cards played for less than this many minutes.}
}
\value{
A data frame with one row per square per card: \code{card_id},
\code{submission_id}, \code{received_at}, \code{mode}, \code{age}, \code{park}, \code{difficulty}
(the card's settings), \code{started_at}, \code{ended_at}, \code{minutes} (time played),
\code{id} (the square id), and \code{crossed} (logical).
}
\description{
Reads the results players shared from the phone app when they tapped
"Done playing", from the Google Sheet inbox described in
\code{backend/README.md}. Each row is one square on one card, ready for
\code{\link[=update_difficulty]{update_difficulty()}}. Requires the curl package.
}
\details{
Cards finished within a few minutes of being dealt usually weren't really
played, so consider setting \code{min_minutes}.
}
\examples{
\dontrun{
results <- fetch_results(min_minutes = 30)
squares <- update_difficulty(bingo_squares(), results)
}
}
\seealso{
\code{\link[=update_difficulty]{update_difficulty()}}
}
