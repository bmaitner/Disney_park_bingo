% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggestions.R
\name{fetch_suggestions}
\alias{fetch_suggestions}
\title{Fetch square suggestions from players}
\usage{
fetch_suggestions(
  endpoint = Sys.getenv("PARKBINGO_ENDPOINT"),
  token = Sys.getenv("PARKBINGO_TOKEN")
)
}
\arguments{
\item{endpoint}{The inbox's Apps Script web app URL. Defaults to the
\code{PARKBINGO_ENDPOINT} environment variable.}

\item{token}{The private read token created by \code{createReadToken()} in the
script. Defaults to the \code{PARKBINGO_TOKEN} environment variable.}
}
\value{
A data frame with one row per suggestion: \code{received_at},
\code{submission_id}, \code{text}, \code{mode}, \code{age}, \code{park}, \code{note}, and \code{status} (the
column you fill in while reviewing).
}
\description{
Reads the suggestions players sent from the phone app, from the Google Sheet
inbox described in \code{backend/README.md}. Requires the curl package.
}
\examples{
\dontrun{
suggestions <- fetch_suggestions()
suggestions[suggestions$status == "", ]
}
}
\seealso{
\code{\link[=add_squares]{add_squares()}} to add the ones you like to the squares table.
}
