% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/squares.R
\name{bingo_squares}
\alias{bingo_squares}
\title{Bingo squares table}
\usage{
bingo_squares(path = NULL)
}
\arguments{
\item{path}{Path to a \code{.csv}, \code{.csv.gz}, or \code{.parquet} file. \code{NULL} (the
default) uses the table shipped with the package.}
}
\value{
A data frame with the columns described below.
}
\description{
Reads the table of candidate bingo squares. By default this is the table
shipped with the package (\code{inst/extdata/bingo_squares.csv}), but any CSV
(optionally gzipped) or Parquet file with the same columns can be used.
}
\section{Columns}{

\describe{
\item{id}{Stable unique identifier (e.g. \code{"sq0001"}). Used to link player
feedback back to squares, so never reuse or renumber ids.}
\item{text}{Label printed on the card. Keep it short (roughly 40
characters or fewer).}
\item{mode}{\code{"cynic"}, \code{"fan"}, or \code{"any"} (fits both).}
\item{age}{\code{"child"} (suitable for everyone) or \code{"adult"} (adults only).}
\item{park}{Where the square can occur: \code{"any"}, or one or more park codes
separated by \verb{;} (e.g. \code{"magic_kingdom;epcot"}). See \code{\link[=square_vocab]{square_vocab()}}.}
\item{category}{Free-form grouping such as \code{people}, \code{outfits}, \code{food},
\code{rides}.}
\item{p_crossed}{Estimated proportion of players who cross the square off
during a typical park day (0-1). Higher is easier; difficulty is
\code{1 - p_crossed}. Starts as a guess and is refined with
\code{\link[=update_difficulty]{update_difficulty()}}.}
\item{n_feedback}{Number of player observations that have informed
\code{p_crossed} (0 for an untested guess).}
}
}

\examples{
squares <- bingo_squares()
table(squares$mode, squares$age)
}
\seealso{
\code{\link[=write_squares]{write_squares()}}, \code{\link[=validate_squares]{validate_squares()}}
}
