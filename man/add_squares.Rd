% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggestions.R
\name{add_squares}
\alias{add_squares}
\title{Add squares to the squares table}
\usage{
add_squares(squares, new, p_crossed = 0.5)
}
\arguments{
\item{squares}{A squares table, e.g. from \code{\link[=bingo_squares]{bingo_squares()}}.}

\item{new}{A data frame with columns \code{text}, \code{mode}, \code{age}, \code{park}, and
\code{category}, and optionally \code{p_crossed} (starting difficulty guess) and
\code{n_feedback}. Other columns, like those from \code{\link[=fetch_suggestions]{fetch_suggestions()}}, are
ignored.}

\item{p_crossed}{Starting \code{p_crossed} for rows that don't have one.}
}
\value{
The squares table with the new rows at the end.
}
\description{
Appends new squares, e.g. reviewed player suggestions, giving each the next
free id. The result is validated.
}
\examples{
new <- data.frame(
  text = "Stroller with a license plate", mode = "any", age = "child",
  park = "any", category = "people"
)
tail(add_squares(bingo_squares(), new), 2)
}
