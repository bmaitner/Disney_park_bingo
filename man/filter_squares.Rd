% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/card.R
\name{filter_squares}
\alias{filter_squares}
\title{Filter squares for a card}
\usage{
filter_squares(
  squares = bingo_squares(),
  mode = c("cynic", "fan", "mixed"),
  age = c("adult", "child"),
  park = "any",
  difficulty = "any"
)
}
\arguments{
\item{squares}{A squares table, e.g. from \code{\link[=bingo_squares]{bingo_squares()}}.}

\item{mode}{\code{"cynic"} or \code{"fan"} keep squares of that mode plus those tagged
\code{"any"}; \code{"mixed"} keeps all modes.}

\item{age}{\code{"adult"} keeps all squares; \code{"child"} keeps only child-suitable
squares.}

\item{park}{A park name or code (see \code{\link[=park_code]{park_code()}}). \code{"any"} keeps only
squares that can happen anywhere; a specific park adds its own squares.}

\item{difficulty}{\code{"any"} (no filter), \code{"easy"} (\code{p_crossed >= 0.6}),
\code{"medium"} (0.3-0.6), \code{"hard"} (\code{p_crossed < 0.3}), or a numeric
\code{c(min, max)} range of \code{p_crossed}.}
}
\value{
The filtered squares table.
}
\description{
Selects the squares eligible for a card with the given mode, age, park, and
difficulty.
}
\examples{
nrow(filter_squares(bingo_squares(), mode = "fan", age = "child", park = "EPCOT"))
}
