% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/difficulty.R
\name{bingo_odds}
\alias{bingo_odds}
\title{Estimate a card's odds of a bingo}
\usage{
bingo_odds(card, n_sim = 10000)
}
\arguments{
\item{card}{A \code{bingo_card}.}

\item{n_sim}{Number of simulated park days.}
}
\value{
A list with \code{p_bingo} (probability of at least one bingo),
\code{expected_crossed} (expected number of squares crossed, including the
free space), and \code{mean_p_crossed} (average \code{p_crossed} of the drawn
squares).
}
\description{
Simulates play by crossing off each square independently with probability
\code{p_crossed} (the free space is always crossed), and reports how often the
card gets at least one full row, column, or diagonal.
}
\examples{
card <- make_bingo_card(seed = 3)
bingo_odds(card)
}
